# Firmware build outputs

This folder is expected to contain compiled firmware binaries (.bin/.elf/.hex). Do not commit large build artifacts to the repository unless necessary; prefer releases or build artifacts storage. Keep this README updated with how to rebuild the firmware.
